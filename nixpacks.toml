providers = ["python", "node"]

[phases.setup]
nixPkgs = ["pkgs.python312", "pkgs.nodejs_20"]

[phases.install]
cmds = [
  "npm install",
  "pip install -r python_sim/requirements.txt"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "chmod +x start.sh && ./start.sh"

